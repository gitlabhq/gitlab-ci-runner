#!/usr/bin/env ruby
require 'rubygems'
require 'bundler/setup'

ROOT_PATH = File.expand_path(File.join(File.dirname(__FILE__), ".."))

require File.join(ROOT_PATH, 'lib', 'runner')

GitlabCi::Runner.new
exit
